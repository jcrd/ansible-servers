- hosts: localserver
  vars:
    homedir: "{{ lookup('env', 'HOME') }}"
    configdir: '{{ homedir }}/.config'
  roles:
    - base
    - dotfiles
    - deluge
    - openvpn_client
    - plex
    - radarr
    - syncthing
